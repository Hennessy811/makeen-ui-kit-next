import {
  Meta,
  Story,
  Canvas,
  Preview,
  Props,
} from "@storybook/addon-docs/blocks";
import { Card, Box, Typography } from "@material-ui/core";

import Button from "./Button";

<Meta title="Makeen UI-Kit/Button" component={Button} />

# Advanced usage

With `MDX` we can define a story with samples that just works

<Preview>
  <Story name="AdvancedUsage">
    <Card>
      <Box p={4}>
        <Typography>sample</Typography>
        <Button variant="outlined" color="primary">
          Kek
        </Button>
      </Box>
    </Card>
  </Story>
</Preview>

# Props

<Props story="AdvancedUsage" />
